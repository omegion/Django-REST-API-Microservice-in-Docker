#!groovy

node {

    try {
        stage 'Checkout'
            sh 'ls'

        stage 'Test'
            sh 'ls'

        stage 'Deploy'
            sh 'ls'

        stage 'Publish results'
            sh 'ls'
    }

    catch (err) {

        throw err
    }

}
